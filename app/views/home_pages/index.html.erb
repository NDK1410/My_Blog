<% @categories.each do |category| %>
  <h1><%= link_to category.name, category_path(category) %></h1>
  <% category.posts.each do |post| %>
    <strong><%= link_to post.title, category_post_path(category, post) %></strong>
    <p><%= post.description %></p>
  <% end %>
<% end %>
